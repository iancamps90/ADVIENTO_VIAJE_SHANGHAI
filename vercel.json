{
    "version": 2,
    "buildCommand": "pip3 install -r requirements.txt && python3 -m reflex export --frontend-only",
    "outputDirectory": "web/_static",
    "installCommand": "pip3 install -r requirements.txt"
}